include ../section/section
include ../contact/contact

mixin contact-list(options)
    .contact-list
        +section({ title: "contact", icon: "contact"})
            ul.contact-list__items
                each option in options
                    li.contact-list__item
                        +contact(option)