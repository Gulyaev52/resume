include ../skill/skill

mixin skill-list(options)
    .skill-list
        +section({ title: "skills", icon: "skill" })
            ul.skill-list__items
                each option in options
                    li.skill-list__item
                        +skill(option)