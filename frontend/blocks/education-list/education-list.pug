include ../education/education

mixin education-list(options)
    .education-list
        +section({ title: "education", icon: "education" })
            each option in options
                .education-list__item
                    +education(option)